openapi: 3.0.3
info:
  title: stella API Components
  version: "1"
  description: Assembled from api/spec/components/common.yaml + api/spec/domain/*/schemas.yaml — DO NOT EDIT
paths: {}
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      description: STELLA_TOKEN bearer token (generated via vault)
  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message, status]
          properties:
            code:
              type: integer
              description: mirrors the HTTP status code
            message:
              type: string
              description: human-readable, actionable description
            status:
              type: string
              description: canonical status name (e.g. NOT_FOUND, INVALID_ARGUMENT)
            details:
              type: object
              additionalProperties: true
              description: optional machine-readable error details; keys use snake_case
    SandboxNetworkConfig:
      type: object
      properties:
        mode:
          type: string
        allowlist:
          type: array
          items:
            type: string
    SandboxConfig:
      type: object
      properties:
        network:
          $ref: "#/components/schemas/SandboxNetworkConfig"
    Agent:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        model:
          type: string
        model_strong:
          type: string
        model_fast:
          type: string
        system_prompt:
          type: string
        soul:
          type: string
        workspace:
          type: string
        sandbox:
          $ref: "#/components/schemas/SandboxConfig"
        scope:
          type: string
        creator_id:
          type: string
        enabled:
          type: boolean
    AgentList:
      type: object
      required: [agents]
      properties:
        agents:
          type: array
          items:
            $ref: "#/components/schemas/Agent"
    CreateAgentRequest:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        model:
          type: string
        model_strong:
          type: string
        model_fast:
          type: string
        system_prompt:
          type: string
        soul:
          type: string
        workspace:
          type: string
        sandbox:
          $ref: "#/components/schemas/SandboxConfig"
        scope:
          type: string
        creator_id:
          type: string
        enabled:
          type: boolean
        template_id:
          type: string
    AgentUser:
      type: object
      properties:
        id:
          type: string
        username:
          type: string
    AgentUserList:
      type: object
      required: [users]
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/AgentUser"
    AssignAgentUserRequest:
      type: object
      required: [user_id]
      properties:
        user_id:
          type: string
    Skill:
      type: object
      properties:
        id:
          type: string
        scope:
          type: string
          enum: [project, user, agent, system]
        user_id:
          type: string
        agent_id:
          type: string
        name:
          type: string
        description:
          type: string
        status:
          type: string
        disable_model_invocation:
          type: boolean
        files:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    SkillList:
      type: object
      required: [skills]
      properties:
        skills:
          type: array
          items:
            $ref: "#/components/schemas/Skill"
    CreateSkillRequest:
      type: object
      required: [name, scope]
      properties:
        name:
          type: string
        scope:
          type: string
          enum: [user, agent]
        description:
          type: string
        status:
          type: string
        disable_model_invocation:
          type: boolean
        files:
          type: object
          additionalProperties:
            type: string
    UpdateSkillRequest:
      type: object
      properties:
        description:
          type: string
          nullable: true
        status:
          type: string
          nullable: true
        disable_model_invocation:
          type: boolean
          nullable: true
        files:
          type: object
          additionalProperties:
            type: string
    InstallSkillRequest:
      type: object
      required: [source]
      properties:
        source:
          type: string
        scope:
          type: string
          enum: [user, agent]
    SkillFileResponse:
      type: object
      properties:
        path:
          type: string
        content:
          type: string
    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
    RegisterRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
    AuthResponse:
      type: object
      required: [id, username]
      properties:
        id:
          type: string
        username:
          type: string
    MeResponse:
      type: object
      required: [id, username, role, is_admin]
      properties:
        id:
          type: string
        username:
          type: string
        role:
          type: string
        is_admin:
          type: boolean
        email:
          type: string
        name:
          type: string
        avatar_url:
          type: string
        has_credentials:
          type: boolean
    AuthSession:
      type: object
      required: [id, expires_at, created_at]
      properties:
        id:
          type: string
        expires_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    AuthSessionList:
      type: object
      required: [sessions]
      properties:
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/AuthSession"
    OIDCProvider:
      type: object
      required: [name, login_url]
      properties:
        name:
          type: string
          description: Human-readable provider name (e.g. "Zitadel")
        login_url:
          type: string
          description: URL to redirect the browser to for login
        register_url:
          type: string
          description: URL to redirect the browser to for registration (local provider only)
    OIDCProviderList:
      type: object
      required: [providers]
      properties:
        providers:
          type: array
          items:
            $ref: "#/components/schemas/OIDCProvider"
    BuiltinResource:
      type: object
      required: [kind, id, name]
      properties:
        kind:
          type: string
        id:
          type: string
        name:
          type: string
        description:
          type: string
        tags:
          type: array
          items:
            type: string
        metadata:
          type: object
          additionalProperties: true
        hash:
          type: string
    BuiltinResourceDetail:
      type: object
      required: [kind, id, name, content]
      properties:
        kind:
          type: string
        id:
          type: string
        name:
          type: string
        description:
          type: string
        tags:
          type: array
          items:
            type: string
        metadata:
          type: object
          additionalProperties: true
        hash:
          type: string
        content:
          type: string
    BuiltinResourceList:
      type: object
      required: [resources]
      properties:
        resources:
          type: array
          items:
            $ref: "#/components/schemas/BuiltinResource"
    Channel:
      type: object
      required: [id, name, type, enabled, config]
      properties:
        id:
          type: string
        name:
          type: string
        type:
          type: string
        agent_id:
          type: string
        enabled:
          type: boolean
        config:
          type: string
          description: "JSON string of the channel config"
    ChannelList:
      type: object
      required: [channels]
      properties:
        channels:
          type: array
          items:
            $ref: "#/components/schemas/Channel"
    PublicChannel:
      type: object
      required: [id, type, label, enabled]
      properties:
        id:
          type: string
        type:
          type: string
        label:
          type: string
        agent_id:
          type: string
        agent_name:
          type: string
        enabled:
          type: boolean
    PublicChannelList:
      type: object
      required: [channels]
      properties:
        channels:
          type: array
          items:
            $ref: "#/components/schemas/PublicChannel"
    ChannelWriteRequest:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        type:
          type: string
          nullable: true
        agent_id:
          type: string
          nullable: true
        enabled:
          type: boolean
          nullable: true
        config:
          type: string
          description: "JSON string of the channel config"
    WeixinQRCode:
      type: object
      properties:
        qrcode:
          type: string
        qrcode_img_content:
          type: string
    WeixinQRStatus:
      type: object
      properties:
        status:
          type: string
        bot_token:
          type: string
        ilink_bot_id:
          type: string
        ilink_user_id:
          type: string
        baseurl:
          type: string
    Goal:
      type: object
      required: [
        id,
        user_id,
        title,
        status,
        priority,
        review_policy,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        user_id:
          type: string
        agent_id:
          type: string
        title:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [
            draft,
            planning,
            running,
            blocked,
            reviewing,
            done,
            failed,
            cancelled,
          ]
        priority:
          type: string
          enum: [routine, urgent]
        review_policy:
          type: string
          enum: [none, auto, agent, human]
        active_review_id:
          type: string
        context:
          type: object
          additionalProperties: {}
        output:
          type: object
          additionalProperties: {}
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        cancelled_at:
          type: string
          format: date-time
    GoalList:
      type: object
      required: [goals]
      properties:
        goals:
          type: array
          items:
            $ref: "#/components/schemas/Goal"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    CreateGoalRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
        description:
          type: string
        priority:
          type: string
          enum: [routine, urgent]
        agent_id:
          type: string
        review_policy:
          type: string
          enum: [none, auto, agent, human]
        context:
          type: object
          additionalProperties: {}
    CancelGoalRequest:
      type: object
      properties:
        reason:
          type: string
    CachedModel:
      type: object
      required: [provider, model]
      properties:
        provider:
          type: string
        provider_name:
          type: string
        model:
          type: string
    CachedModelList:
      type: object
      required: [models]
      properties:
        models:
          type: array
          items:
            $ref: "#/components/schemas/CachedModel"
    PluginView:
      type: object
      properties:
        id:
          type: string
        kind:
          type: string
        name:
          type: string
        enabled:
          type: boolean
        config:
          type: object
          additionalProperties: true
        display_name:
          type: string
        description:
          type: string
        managed:
          type: boolean
        admin_visible:
          type: boolean
        has_config:
          type: boolean
        has_status:
          type: boolean
        capabilities:
          type: array
          items:
            type: string
        persisted:
          type: boolean
        persisted_id:
          type: string
    TogglePluginRequest:
      type: object
      required: [enabled]
      properties:
        enabled:
          type: boolean
    UpdatePluginConfigRequest:
      type: object
      properties:
        config:
          type: object
          additionalProperties: true
    ManifestPlugin:
      type: object
      additionalProperties: true
    ManifestOAuthProvider:
      type: object
      properties:
        id:
          type: string
        scopes:
          type: array
          items:
            type: string
        vault_key:
          type: string
        client_id:
          type: string
    ManifestPluginsResponse:
      type: object
      properties:
        plugins:
          type: array
          items:
            $ref: "#/components/schemas/ManifestPlugin"
        oauth_providers:
          type: array
          items:
            $ref: "#/components/schemas/ManifestOAuthProvider"
    PluginViewList:
      type: object
      required: [plugins]
      properties:
        plugins:
          type: array
          items:
            $ref: "#/components/schemas/PluginView"
    ChangePasswordRequest:
      type: object
      required: [current_password, new_password]
      properties:
        current_password:
          type: string
        new_password:
          type: string
    GenerateLinkCodeRequest:
      type: object
      required: [platform]
      properties:
        platform:
          type: string
    LinkCodeResponse:
      type: object
      required: [code, platform]
      properties:
        code:
          type: string
        platform:
          type: string
    SetSoulRequest:
      type: object
      required: [soul]
      properties:
        soul:
          type: string
    VaultEntry:
      type: object
      required: [name, created_at, updated_at]
      properties:
        name:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    SetVaultEntryRequest:
      type: object
      required: [value]
      properties:
        value:
          type: string
    VaultEntryValue:
      type: object
      required: [name, value]
      properties:
        name:
          type: string
        value:
          type: string
    OAuthProviderStatus:
      type: object
      required: [provider, available, configured, connected]
      properties:
        provider:
          type: string
        available:
          type: boolean
        configured:
          type: boolean
        connected:
          type: boolean
        username:
          type: string
        unavailable:
          type: string
    OAuthProviderConfig:
      type: object
      required: [provider_id, client_id, client_secret]
      properties:
        provider_id:
          type: string
        client_id:
          type: string
        client_secret:
          type: string
        redirect_url:
          type: string
    OAuthProviderConfigInput:
      type: object
      required: [client_id, client_secret]
      properties:
        client_id:
          type: string
        client_secret:
          type: string
        redirect_url:
          type: string
    OAuthFlowStatus:
      type: object
      required: [provider, flow_id, verification_uri, expires_at, state]
      properties:
        provider:
          type: string
        flow_id:
          type: string
        verification_uri:
          type: string
        user_code:
          type: string
        expires_at:
          type: string
          format: date-time
        state:
          type: string
    OAuthConnectedResponse:
      type: object
      required: [connected]
      properties:
        connected:
          type: boolean
        username:
          type: string
    VaultEntryList:
      type: object
      required: [entries]
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/VaultEntry"
    OAuthProviderStatusList:
      type: object
      required: [providers]
      properties:
        providers:
          type: array
          items:
            $ref: "#/components/schemas/OAuthProviderStatus"
    Project:
      type: object
      required: [
        id,
        agent_id,
        user_id,
        name,
        base_dir,
        archived,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        agent_id:
          type: string
        user_id:
          type: string
        name:
          type: string
        base_dir:
          type: string
        description:
          type: string
        archived:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateProjectRequest:
      type: object
      required: [name, base_dir]
      properties:
        name:
          type: string
        base_dir:
          type: string
        description:
          type: string
    UpdateProjectRequest:
      type: object
      properties:
        name:
          type: string
        base_dir:
          type: string
        description:
          type: string
    ProjectList:
      type: object
      required: [projects]
      properties:
        projects:
          type: array
          items:
            $ref: "#/components/schemas/Project"
    ProviderModelCost:
      type: object
      properties:
        input:
          type: number
          format: double
        output:
          type: number
          format: double
        cacheRead:
          type: number
          format: double
        cacheWrite:
          type: number
          format: double
    ProviderModel:
      type: object
      required: [enabled]
      properties:
        id:
          type: string
        name:
          type: string
        enabled:
          type: boolean
        reasoning:
          type: boolean
        input:
          type: array
          items:
            type: string
        output:
          type: array
          items:
            type: string
        contextWindow:
          type: integer
        maxTokens:
          type: integer
        cost:
          $ref: "#/components/schemas/ProviderModelCost"
    Provider:
      type: object
      required: [id, type, name, enabled, api_key, base_url]
      properties:
        id:
          type: string
        type:
          type: string
        name:
          type: string
        enabled:
          type: boolean
        api_key:
          type: string
        base_url:
          type: string
        models:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/ProviderModel"
    ProviderModelItem:
      type: object
      required: [id, source, enabled]
      properties:
        id:
          type: string
        name:
          type: string
        source:
          type: string
        enabled:
          type: boolean
        config:
          $ref: "#/components/schemas/ProviderModel"
    ProviderType:
      type: object
      required: [id, name, default_url]
      properties:
        id:
          type: string
        name:
          type: string
        default_url:
          type: string
    FetchModelsRequest:
      type: object
      properties:
        api_key:
          type: string
        base_url:
          type: string
    ProviderList:
      type: object
      required: [providers]
      properties:
        providers:
          type: array
          items:
            $ref: "#/components/schemas/Provider"
    ProviderModelList:
      type: object
      required: [models]
      properties:
        models:
          type: array
          items:
            $ref: "#/components/schemas/ProviderModelItem"
    ProviderTypeList:
      type: object
      required: [provider_types]
      properties:
        provider_types:
          type: array
          items:
            $ref: "#/components/schemas/ProviderType"
    # ── Recally ────────────────────────────────────────────────────────────────
    ArticleStatus:
      type: string
      enum: [unread, read, archived]
    SourceType:
      type: string
      enum: [web, twitter, youtube, github, rss, pdf]
    FeedEntryStatus:
      type: string
      enum: [pending, saved, skipped, error]
    Article:
      type: object
      required: [
        id,
        url,
        canonical_url,
        source_type,
        title,
        status,
        starred,
        file_path,
        saved_at,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        agent_id:
          type: string
          nullable: true
        url:
          type: string
          format: uri
        canonical_url:
          type: string
        source_type:
          $ref: "#/components/schemas/SourceType"
        title:
          type: string
        author:
          type: string
        summary:
          type: string
        tags:
          type: array
          items:
            type: string
        status:
          $ref: "#/components/schemas/ArticleStatus"
        starred:
          type: boolean
        file_path:
          type: string
        metadata:
          type: object
          additionalProperties:
            type: string
        published_at:
          type: string
          format: date-time
          nullable: true
        saved_at:
          type: string
          format: date-time
        read_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        content:
          type: string
          description: "Article body in markdown. Returned only when ?include=content is set."
          nullable: true
    ArticleList:
      type: object
      required: [articles]
      properties:
        articles:
          type: array
          items:
            $ref: "#/components/schemas/Article"
        next_page_token:
          type: string
          nullable: true
    SaveArticleRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        canonical_url:
          type: string
        source_type:
          $ref: "#/components/schemas/SourceType"
        title:
          type: string
        author:
          type: string
        summary:
          type: string
        tags:
          type: array
          items:
            type: string
        content:
          type: string
          description: "Markdown body. If empty and the article exists, only metadata is updated; if empty and the article is new, the request fails 400."
        metadata:
          type: object
          additionalProperties:
            type: string
        published_at:
          type: string
          format: date-time
          nullable: true
        agent_id:
          type: string
          nullable: true
    UpdateArticleRequest:
      type: object
      description: "Only provided fields are updated."
      properties:
        title:
          type: string
        author:
          type: string
        summary:
          type: string
        tags:
          type: array
          items:
            type: string
        status:
          $ref: "#/components/schemas/ArticleStatus"
        starred:
          type: boolean
        file_path:
          type: string
        metadata:
          type: object
          additionalProperties:
            type: string
        published_at:
          type: string
          format: date-time
          nullable: true
        content:
          type: string
          description: "If set, rewrites the markdown file body."
    Feed:
      type: object
      required: [
        id,
        url,
        title,
        check_interval,
        enabled,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        agent_id:
          type: string
          nullable: true
        url:
          type: string
          format: uri
        title:
          type: string
        description:
          type: string
        check_interval:
          type: string
        last_checked_at:
          type: string
          format: date-time
          nullable: true
        last_etag:
          type: string
        last_modified:
          type: string
        enabled:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    FeedList:
      type: object
      required: [feeds]
      properties:
        feeds:
          type: array
          items:
            $ref: "#/components/schemas/Feed"
        next_page_token:
          type: string
          nullable: true
    CreateFeedRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        title:
          type: string
          description: "Optional override; server fetches feed metadata if omitted"
        agent_id:
          type: string
          nullable: true
    UpdateFeedRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        check_interval:
          type: string
        enabled:
          type: boolean
    FeedEntry:
      type: object
      required: [id, feed_id, guid, url, title, status, attempts, discovered_at]
      properties:
        id:
          type: string
        feed_id:
          type: string
        guid:
          type: string
        url:
          type: string
        title:
          type: string
        status:
          $ref: "#/components/schemas/FeedEntryStatus"
        article_id:
          type: string
          nullable: true
        attempts:
          type: integer
        error_msg:
          type: string
        discovered_at:
          type: string
          format: date-time
        processed_at:
          type: string
          format: date-time
          nullable: true
    FeedEntryList:
      type: object
      required: [entries]
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/FeedEntry"
        next_page_token:
          type: string
          nullable: true
    UpdateFeedEntryRequest:
      type: object
      required: [status]
      properties:
        status:
          $ref: "#/components/schemas/FeedEntryStatus"
        article_id:
          type: string
          nullable: true
        error_msg:
          type: string
    FeedPollResult:
      type: object
      required: [feed, new_entries]
      properties:
        feed:
          $ref: "#/components/schemas/Feed"
        new_entries:
          type: array
          items:
            $ref: "#/components/schemas/FeedEntry"
        error:
          type: string
          nullable: true
          description: "Set if the upstream RSS fetch failed but the feed exists"
    TagCount:
      type: object
      required: [tag, count]
      properties:
        tag:
          type: string
        count:
          type: integer
          format: int64
    Digest:
      type: object
      required:
        - date
        - saved_yesterday
        - saved_yesterday_count
        - unread_count
        - read_count
        - archived_count
        - starred_count
        - worth_revisiting
        - worth_revisiting_count
        - top_tags
        - total_articles
      properties:
        date:
          type: string
          format: date-time
        saved_yesterday:
          type: array
          items:
            $ref: "#/components/schemas/Article"
        saved_yesterday_count:
          type: integer
        unread_count:
          type: integer
          format: int64
        read_count:
          type: integer
          format: int64
        archived_count:
          type: integer
          format: int64
        starred_count:
          type: integer
          format: int64
        worth_revisiting:
          type: array
          items:
            $ref: "#/components/schemas/Article"
        worth_revisiting_count:
          type: integer
        top_tags:
          type: array
          items:
            $ref: "#/components/schemas/TagCount"
        total_articles:
          type: integer
          format: int64
    SaveDigestRequest:
      type: object
      required: [narrative]
      properties:
        narrative:
          type: string
        date:
          type: string
          description: "YYYY-MM-DD; defaults to today if omitted"
    StoredDigestSummary:
      type: object
      required: [
        id,
        date,
        narrative,
        saved_yesterday_count,
        worth_revisiting_count,
        total_articles,
        created_at,
      ]
      properties:
        id:
          type: string
        date:
          type: string
        narrative:
          type: string
        saved_yesterday_count:
          type: integer
          format: int64
        worth_revisiting_count:
          type: integer
          format: int64
        total_articles:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
    StoredDigestSummaryList:
      type: object
      required: [digests, total_size]
      properties:
        digests:
          type: array
          items:
            $ref: "#/components/schemas/StoredDigestSummary"
        total_size:
          type: integer
          format: int64
        next_page_token:
          type: string
          nullable: true
    StoredDigest:
      type: object
      required: [
        id,
        date,
        narrative,
        saved_yesterday,
        saved_yesterday_count,
        unread_count,
        read_count,
        archived_count,
        starred_count,
        worth_revisiting,
        worth_revisiting_count,
        top_tags,
        total_articles,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        date:
          type: string
        narrative:
          type: string
        saved_yesterday:
          type: array
          items:
            $ref: "#/components/schemas/Article"
        saved_yesterday_count:
          type: integer
          format: int64
        unread_count:
          type: integer
          format: int64
        read_count:
          type: integer
          format: int64
        archived_count:
          type: integer
          format: int64
        starred_count:
          type: integer
          format: int64
        worth_revisiting:
          type: array
          items:
            $ref: "#/components/schemas/Article"
        worth_revisiting_count:
          type: integer
          format: int64
        top_tags:
          type: array
          items:
            $ref: "#/components/schemas/TagCount"
        total_articles:
          type: integer
          format: int64
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    Job:
      type: object
      required: [id, name, message, session_mode, enabled]
      properties:
        id:
          type: string
        owner_kind:
          type: string
          description: "Ownership class: 'user' or 'plugin'"
        plugin_id:
          type: string
        job_key:
          type: string
        runtime_name:
          type: string
        name:
          type: string
        description:
          type: string
        cron:
          type: string
          description: "Cron expression (mutually exclusive with every/at)"
        every:
          type: string
          description: "Go duration string, e.g. '30m' (mutually exclusive with cron/at)"
        at:
          type: string
          description: "RFC3339 one-time timestamp (mutually exclusive with cron/every)"
        message:
          type: string
          description: "Prompt/instruction to run on schedule"
        payload:
          type: object
          additionalProperties: {}
          description: "Arbitrary structured data for plugin-owned jobs"
        session_mode:
          type: string
          description: "Session behaviour: 'reuse' or 'new'"
        enabled:
          type: boolean
        agent_id:
          type: string
        user_id:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        last_run_at:
          type: string
          format: date-time
          nullable: true
        last_error:
          type: string
    JobList:
      type: object
      required: [jobs]
      properties:
        jobs:
          type: array
          items:
            $ref: "#/components/schemas/Job"
    JobInput:
      type: object
      description: "Fields for creating or updating a job. For create: name, message, and exactly one of cron/every/at are required."
      properties:
        name:
          type: string
        description:
          type: string
        cron:
          type: string
        every:
          type: string
        at:
          type: string
        message:
          type: string
        session_mode:
          type: string
        enabled:
          type: boolean
        agent_id:
          type: string
        user_id:
          type: string
          description: "Admin-only: set empty for system jobs"
    JobRun:
      type: object
      required: [id, job_id, session_id, status, started_at]
      properties:
        id:
          type: string
        job_id:
          type: string
        session_id:
          type: string
        user_id:
          type: string
        status:
          type: string
        started_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
        error:
          type: string
        duration:
          type: string
    JobRunList:
      type: object
      required: [runs]
      properties:
        runs:
          type: array
          items:
            $ref: "#/components/schemas/JobRun"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    TriggerJobResult:
      type: object
      required: [run_id]
      properties:
        run_id:
          type: string
    DeleteResult:
      type: object
      required: [status]
      properties:
        status:
          type: string
    Session:
      type: object
      required: [
        id,
        channel,
        kind,
        title,
        agent_id,
        user_id,
        created_at,
        last_active,
        archived,
      ]
      properties:
        id:
          type: string
        channel:
          type: string
        kind:
          type: string
          enum: [main, chat, scheduler, task]
        project_id:
          type: string
        title:
          type: string
        agent_id:
          type: string
        user_id:
          type: string
        created_at:
          type: string
          format: date-time
        last_active:
          type: string
          format: date-time
        archived:
          type: boolean
    SessionDetail:
      allOf:
        - $ref: "#/components/schemas/Session"
        - type: object
          properties:
            agent_name: { type: string }
            user_name: { type: string }
    CreateSessionRequest:
      type: object
      properties:
        agent_id:
          type: string
        kind:
          type: string
          enum: [main, chat, scheduler, task]
        project_id:
          type: string
    MessagePart:
      type: object
      required: [type]
      properties:
        type:
          type: string
          enum: [text, image, file]
        text:
          type: string
          description: Text content (when type is "text")
        image:
          type: string
          description: Base64-encoded image data (when type is "image")
        url:
          type: string
          description: File URL (when type is "file")
        mimeType:
          type: string
          description: MIME type of the image or file
    SendMessageRequest:
      type: object
      required: [parts]
      properties:
        parts:
          type: array
          items:
            $ref: "#/components/schemas/MessagePart"
          description: Message content parts (text, image, file)
    SystemPromptResponse:
      type: object
      required: [system_prompt]
      properties:
        system_prompt:
          type: string
    SessionWorkspace:
      type: object
      required: [root, paths, total_files, total_dirs, total_bytes]
      properties:
        root:
          type: string
          description: Absolute path of the workspace root directory
        paths:
          type: array
          items:
            type: string
          description: Relative file and directory paths within the workspace. Directory paths end with a slash.
        total_files:
          type: integer
          description: Number of files in the workspace
        total_dirs:
          type: integer
          description: Number of directories in the workspace, excluding the root
        total_bytes:
          type: integer
          format: int64
          description: Total size of regular files in bytes
    WorkspaceCreateRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
          description: Relative path for the new file or directory
        is_dir:
          type: boolean
          description: Create a directory when true; a file when false or omitted
        content:
          type: string
          description: Initial file content (ignored when is_dir is true)
    WorkspaceDeleteRequest:
      type: object
      required: [path]
      properties:
        path:
          type: string
          description: Relative path of the file or directory to delete
    WorkspaceMoveRequest:
      type: object
      required: [path, new_path]
      properties:
        path:
          type: string
          description: Current relative path
        new_path:
          type: string
          description: New relative path (rename or move destination)
    WorkspaceFileContent:
      type: object
      required: [path, content]
      properties:
        path:
          type: string
          description: Relative path of the file within the workspace
        content:
          type: string
          description: File text content
        language:
          type: string
          description: Detected language hint for syntax highlighting
    WorkspaceUpdateContentRequest:
      type: object
      required: [path, content]
      properties:
        path:
          type: string
          description: Relative path of the file within the workspace
        content:
          type: string
          description: New file content to write
    WorkspaceUploadResponse:
      type: object
      required: [path]
      properties:
        path:
          type: string
          description: Relative path of the uploaded file within the workspace
    SessionList:
      type: object
      required: [sessions]
      properties:
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/Session"
        next_page_token:
          type: string
          nullable: true
    SessionMessageList:
      type: object
      required: [messages]
      properties:
        messages:
          type: array
          items:
            type: object
            additionalProperties: {}
    Share:
      type: object
      required: [id, url, title, media_type, created_at]
      properties:
        id:
          type: string
        url:
          type: string
        title:
          type: string
        media_type:
          type: string
        expires_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
    CreateShareRequest:
      type: object
      required: [source]
      properties:
        source:
          type: string
          enum: [artifact, article]
        agent_id:
          type: string
        session_id:
          type: string
        path:
          type: string
        article_id:
          type: string
        expires_in:
          type: string
          enum: [1h, 1d, 7d, never]
          default: 7d
    ShareList:
      type: object
      required: [shares]
      properties:
        shares:
          type: array
          items:
            $ref: "#/components/schemas/Share"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    SkillSearchResult:
      type: object
      properties:
        id:
          type: string
        skillId:
          type: string
        name:
          type: string
        installs:
          type: integer
        source:
          type: string
    SkillSearchResultList:
      type: object
      required: [skills]
      properties:
        skills:
          type: array
          items:
            $ref: "#/components/schemas/SkillSearchResult"
    StatusRuntime:
      type: object
      required: [goVersion, os, arch, goroutines, memory]
      properties:
        goVersion:
          type: string
          example: go1.25.0
        os:
          type: string
          example: darwin
        arch:
          type: string
          example: arm64
        goroutines:
          type: integer
          example: 42
        memory:
          $ref: "#/components/schemas/StatusMemory"
    StatusMemory:
      type: object
      required: [allocBytes, heapAllocBytes, sysBytes, numGC]
      properties:
        allocBytes:
          type: integer
          format: int64
        heapAllocBytes:
          type: integer
          format: int64
        sysBytes:
          type: integer
          format: int64
        numGC:
          type: integer
    StatusDatabase:
      type: object
      required: [status]
      properties:
        status:
          type: string
          example: ok
        latencyMs:
          type: number
          format: double
        error:
          type: string
    StatusPlugins:
      type: object
      required: [total, enabled, disabled]
      properties:
        total:
          type: integer
        enabled:
          type: integer
        disabled:
          type: integer
    StatusResponse:
      type: object
      required: [status, version]
      properties:
        status:
          type: string
          example: ok
        version:
          type: string
          example: 0.1.0
        commit:
          type: string
          example: abc1234
        buildDate:
          type: string
          example: "2026-05-09T12:34:56Z"
        uptimeSeconds:
          type: integer
          format: int64
        runtime:
          $ref: "#/components/schemas/StatusRuntime"
        database:
          $ref: "#/components/schemas/StatusDatabase"
        plugins:
          $ref: "#/components/schemas/StatusPlugins"
    Task:
      type: object
      required: [
        id,
        user_id,
        title,
        status,
        priority,
        review_policy,
        required,
        retry_count,
        max_retries,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        user_id:
          type: string
        agent_id:
          type: string
          description: Creator agent (D12); empty when the task was created by a user.
        goal_id:
          type: string
          description: Parent goal (Slice 3); always empty in the flat-API PR.
        title:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [
            draft,
            ready,
            running,
            blocked,
            reviewing,
            done,
            failed,
            cancelled,
          ]
        priority:
          type: string
          enum: [routine, urgent]
        review_policy:
          type: string
          enum: [none, auto, agent, human]
        required:
          type: boolean
        retry_count:
          type: integer
          format: int64
        max_retries:
          type: integer
          format: int64
        not_before:
          type: string
          format: date-time
        deadline_at:
          type: string
          format: date-time
        session_id:
          type: string
        active_run_id:
          type: string
        active_blocker_id:
          type: string
        active_review_id:
          type: string
        context:
          type: object
          additionalProperties: {}
          description: Task-level metadata checkpoint (JSON object).
        output:
          type: object
          additionalProperties: {}
          description: Final output set by the worker on submit (JSON object).
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        cancelled_at:
          type: string
          format: date-time
    TaskList:
      type: object
      required: [tasks]
      properties:
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/Task"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    DepInput:
      type: object
      required: [dep_task_id]
      properties:
        dep_task_id:
          type: string
        kind:
          type: string
          enum: [hard, soft]
          description: Defaults to hard when omitted.
        on_failure:
          type: string
          enum: [block, fail, ignore]
          description: Defaults to block when omitted.
    CreateTaskRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
        description:
          type: string
        priority:
          type: string
          enum: [routine, urgent]
        agent_id:
          type: string
          description: Creator agent (optional).
        executor_agent_id:
          type: string
          description: Explicit executor (D13); written as a dispatch hint.
        required:
          type: boolean
          description: Defaults to true when omitted; false marks the task as optional.
        max_retries:
          type: integer
          format: int64
        not_before:
          type: string
          format: date-time
        deadline_at:
          type: string
          format: date-time
        context:
          type: object
          additionalProperties: {}
        deps:
          type: array
          items:
            $ref: "#/components/schemas/DepInput"
        activate_on_create:
          type: boolean
          description: If true, immediately transitions draft → ready.
    CancelRequest:
      type: object
      properties:
        reason:
          type: string
    Readiness:
      type: object
      required: [state, dispatchable]
      properties:
        state:
          type: string
          enum: [
            dispatchable,
            waiting_deps,
            deferred,
            throttled,
            running,
            blocked,
            terminal,
            draft,
            reviewing,
            unknown,
          ]
        dispatchable:
          type: boolean
        reasons:
          type: array
          items:
            $ref: "#/components/schemas/ReadinessReason"
    ReadinessReason:
      type: object
      required: [type]
      properties:
        type:
          type: string
        upstream_id:
          type: string
        detail:
          type: string
    Run:
      type: object
      required: [
        id,
        task_id,
        user_id,
        kind,
        attempt_no,
        status,
        session_id,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        task_id:
          type: string
        user_id:
          type: string
        agent_id:
          type: string
        executor_agent_id:
          type: string
        kind:
          type: string
          enum: [worker, reviewer, planner, synthesizer]
        attempt_no:
          type: integer
          format: int64
        status:
          type: string
          enum: [
            queued,
            running,
            completed,
            failed,
            cancelled,
            interrupted,
            timed_out,
          ]
        session_id:
          type: string
        input:
          type: string
        result:
          type: string
        error:
          type: string
        worker_id:
          type: string
        heartbeat_at:
          type: string
          format: date-time
        lease_expires_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    RunList:
      type: object
      required: [runs]
      properties:
        runs:
          type: array
          items:
            $ref: "#/components/schemas/Run"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    Event:
      type: object
      required: [id, event_type, actor_type, created_at]
      properties:
        id:
          type: string
        task_id:
          type: string
        run_id:
          type: string
        review_id:
          type: string
        event_type:
          type: string
        from_status:
          type: string
        to_status:
          type: string
        actor_type:
          type: string
          enum: [system, user, agent, worker, reviewer, planner, synthesizer]
        actor_id:
          type: string
        detail:
          type: object
          additionalProperties: {}
        created_at:
          type: string
          format: date-time
    EventList:
      type: object
      required: [events]
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/Event"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    Dep:
      type: object
      required: [task_id, dep_task_id, dep_kind, on_failure, created_at]
      properties:
        task_id:
          type: string
        dep_task_id:
          type: string
        dep_kind:
          type: string
          enum: [hard, soft]
        on_failure:
          type: string
          enum: [block, fail, ignore]
        waived_at:
          type: string
          format: date-time
        waived_by:
          type: string
        waiver_reason:
          type: string
        upstream_status:
          type: string
          description: Convenience-joined current status of the upstream task.
        created_at:
          type: string
          format: date-time
    DepList:
      type: object
      required: [deps]
      properties:
        deps:
          type: array
          items:
            $ref: "#/components/schemas/Dep"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    AddDepRequest:
      type: object
      required: [dep_task_id]
      properties:
        dep_task_id:
          type: string
        kind:
          type: string
          enum: [hard, soft]
        on_failure:
          type: string
          enum: [block, fail, ignore]
    WaiveDepRequest:
      type: object
      properties:
        reason:
          type: string
    ResolveBlockerRequest:
      type: object
      properties:
        resolution:
          type: string
    ReopenRequest:
      type: object
      properties:
        cascade:
          type: boolean
          description: Cascade-reset reachable downstream tasks per ownership rules.
    Review:
      type: object
      required: [
        id,
        reviewer_type,
        status,
        summary,
        feedback,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        task_id:
          type: string
        goal_id:
          type: string
        submitted_run_id:
          type: string
        reviewer_run_id:
          type: string
        reviewer_type:
          type: string
          enum: [system, agent, human]
        reviewer_user_id:
          type: string
        escalated_from_review_id:
          type: string
        status:
          type: string
          enum: [
            requested,
            in_progress,
            approved,
            changes_requested,
            rejected,
            escalated,
            cancelled,
          ]
        summary:
          type: string
        feedback:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        resolved_at:
          type: string
          format: date-time
    ReviewList:
      type: object
      required: [reviews]
      properties:
        reviews:
          type: array
          items:
            $ref: "#/components/schemas/Review"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    ReviewDecisionRequest:
      type: object
      properties:
        summary:
          type: string
        feedback:
          type: string
          description: Ignored on approve; required (by convention) on reject and request_changes.
    EscalateReviewRequest:
      type: object
      properties:
        reason:
          type: string
    Tool:
      type: object
      required: [name, description, category]
      properties:
        name:
          type: string
        description:
          type: string
        input_schema:
          type: object
          additionalProperties: true
        category:
          type: string
    ToolList:
      type: object
      required: [tools]
      properties:
        tools:
          type: array
          items:
            $ref: "#/components/schemas/Tool"
    Identity:
      type: object
      required: [id, user_id, platform, external_id, name, linked_at]
      properties:
        id:
          type: string
        user_id:
          type: string
        platform:
          type: string
        external_id:
          type: string
        name:
          type: string
        linked_at:
          type: string
          format: date-time
    AuthUser:
      type: object
      required: [
        id,
        email,
        name,
        role,
        is_active,
        identities,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
        role:
          type: string
        is_active:
          type: boolean
        identities:
          type: array
          items:
            $ref: "#/components/schemas/ChannelIdentity"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AuthUserList:
      type: object
      required: [users]
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/AuthUser"
        next_page_token:
          type: string
          nullable: true
          description: Opaque pagination token; null when no further pages.
    UserMemory:
      type: object
      additionalProperties: {}
    UpdateDefaultAgentRequest:
      type: object
      required: [default_agent_id]
      properties:
        default_agent_id:
          type: string
    UpdateNotifyIdentityRequest:
      type: object
      properties:
        notify_identity_id:
          type: string
          nullable: true
    SetMemoryRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string
    UpdateRoleRequest:
      type: object
      required: [role]
      properties:
        role:
          type: string
    UpdateAgentsRequest:
      type: object
      required: [agent_ids]
      properties:
        agent_ids:
          type: array
          items:
            type: string
    UpdateActiveRequest:
      type: object
      required: [is_active]
      properties:
        is_active:
          type: boolean
    LoginIdentity:
      type: object
      required: [
        id,
        user_id,
        provider,
        provider_subject,
        email,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        user_id:
          type: string
        provider:
          type: string
        provider_subject:
          type: string
        email:
          type: string
        name:
          type: string
        avatar_url:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ChannelIdentity:
      type: object
      required: [
        id,
        user_id,
        platform,
        external_id,
        name,
        created_at,
        updated_at,
      ]
      properties:
        id:
          type: string
        user_id:
          type: string
        platform:
          type: string
        external_id:
          type: string
        name:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    LinkLoginIdentityRequest:
      type: object
      required: [provider, provider_subject, email]
      properties:
        provider:
          type: string
        provider_subject:
          type: string
        email:
          type: string
        name:
          type: string
    UserMemoryList:
      type: object
      required: [memories]
      properties:
        memories:
          type: array
          items:
            $ref: "#/components/schemas/UserMemory"
    IdentityList:
      type: object
      required: [identities]
      properties:
        identities:
          type: array
          items:
            $ref: "#/components/schemas/Identity"
    LoginIdentityList:
      type: object
      required: [identities]
      properties:
        identities:
          type: array
          items:
            $ref: "#/components/schemas/LoginIdentity"
    ChannelIdentityList:
      type: object
      required: [identities]
      properties:
        identities:
          type: array
          items:
            $ref: "#/components/schemas/ChannelIdentity"
    AuthUserAgentList:
      type: object
      required: [agent_ids]
      properties:
        agent_ids:
          type: array
          items:
            type: string
  responses:
    Unauthorized:
      description: missing or invalid bearer token
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: insufficient permissions
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    BadRequest:
      description: malformed request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: request conflicts with current state
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
